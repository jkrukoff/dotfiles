#!/bin/bash
# .bash_project_utopia_site

UTOPIA_PATH=~/Documents/Utopia\ Site
export VIM_TAGS="$UTOPIA_PATH/tags"

# Only set CDPATH for interactive shells.
if [[ $- == *i* ]]; then
	CDPATH=".:$UTOPIA_PATH"
fi

cd "$UTOPIA_PATH"
ctags --recurse=yes -f - > tags
git fetch
git status
